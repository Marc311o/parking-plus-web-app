import {Box} from '@mui/material';

const PricesPage = () => {
    return (
        <Box
            sx={{
                width: '100%',
                minHeight: '100%',
            }}
        />
    );
};

export default PricesPage;