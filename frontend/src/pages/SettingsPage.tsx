import {Box} from '@mui/material';

const SettingsPage = () => {
    return (
        <Box
            sx={{
                width: '100%',
                minHeight: '100%',
            }}
        />
    );
};

export default SettingsPage;