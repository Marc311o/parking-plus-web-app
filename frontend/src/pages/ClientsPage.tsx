import {Box} from '@mui/material';

const ClientsPage = () => {
    return (
        <Box
            sx={{
                width: '100%',
                minHeight: '100%',
            }}
        />
    );
};

export default ClientsPage;