import {Box} from '@mui/material';

const EventsPage = () => {
    return (
        <Box
            sx={{
                width: '100%',
                minHeight: '100%',
            }}
        />
    );
};

export default EventsPage;