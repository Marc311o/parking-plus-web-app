import {Box} from '@mui/material';

const DashboardPage = () => {
    return (
        <Box
            sx={{
                width: '100%',
                minHeight: '100%',
            }}
        />
    );
};

export default DashboardPage;